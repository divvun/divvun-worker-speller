apiVersion: v1
kind: Service
metadata:
  name: {{ include "divvun-worker-speller.fullname" . }}
  labels:
    {{- include "divvun-worker-speller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "divvun-worker-speller.selectorLabels" . | nindent 4 }}